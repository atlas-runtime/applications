'use strict';

var stringify = require('./stringify');
var parse = require('./parse');
var formats = require('./formats');

let obj = {
    formats: formats,
    parse: parse,
    stringify: stringify
};
module.exports = {obj}
